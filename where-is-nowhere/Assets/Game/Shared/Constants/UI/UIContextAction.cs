namespace Game.Shared.Constants {
    public enum UIContextAction {
        None,
        DefaultCrosshair,
        DefaultAction,
        MenuAction // for when you have multiple possibilities with an interactable
    }
}
